[tox]
envlist = test
#indexserver =
#    default = https://pypi.org/

[testenv]
basepython =
    python2.7
deps =
    pytest
    pytest-cov
    pytest-mock
    -rrequirements.txt

commands =
    test: {envpython} -m pytest -v -s --cov=conport --cov-report term --cov-report html