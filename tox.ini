[tox]
envlist = py{27}

[testenv]
deps =
    pytest
    pytest-cov
    pytest-mock
    -rrequirements.txt

commands =
    test: {envpython} -m pytest -v -s --cov=conport --cov-report term --cov-report html